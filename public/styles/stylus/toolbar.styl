#toolbar
    position fixed
    bottom 0
    left 0
    margin 0 unit($gutter, 'px') unit($gutter, 'px') unit($gutter, 'px')
    .toolbar-control
        opacity .1
        transition opacity .3s ease
        display inline-block
        &:hover
            opacity 1
            cursor pointer